pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "StudySpark"
include(":shared")
include(":webApp")
include(":androidApp")
